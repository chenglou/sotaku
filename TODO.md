# TODO

- Investigate epiplexity as a way to understand the training and loss dynamics behind the current iterative SOTA regime.
- Investigate influence functions to see whether we can identify the most important training examples and reduce data requirements.
- Publish the July 2026 model artifacts as a GitHub release (like the February checkpoint): the 96.5% ES-polished model, the 95.2% and 94.6% harvest rescues (currently on the sudoku-outputs Modal volume and R2), then prune the ~60 study checkpoints from the volume.

## Open explorations (2026-07-05) — threads to keep warm, not verdicts
- Convergence menu: settledness-as-ES-fitness at 2048; randomized-horizon supervised training (no special iteration count); Jacobian damping at the terminal state (the DEQ-stabilizer trick, mechanistically different from the four failed fixed-point losses); DEQ/monotone-operator prototype (guarantee by construction — open research question whether contraction costs capability).
- ES from scratch, phase two: the CE run reached the uniform predictor — the same phase every backprop run passes through before breaking out. Untried knobs to break through: recalibrate sigma at the floor (3e-3 was chosen at random init), bigger population near the floor (signal is tiny there), fitness at horizon 1 first (learn the single-pass skill, then extend — horizon curriculum from the bottom), easy-puzzle fitness curriculum (more givens = denser signal per puzzle).
- Intermediate-horizon ES, take two: fitness at 256 where the 6.7% seed is actually weak (the 64-iteration take failed by design — mastered horizon, no pressure; that was the horizon choice failing, not the idea).
- muP width test: "d=192 collapses at every LR" is the textbook signature of hyperparameters that don't transfer across width; a muP-parameterized testbed run answers whether wider was ever really broken. Needs careful implementation — wrong muP gives a confident wrong answer.
- ES fitness pool: enlarge from 20K puzzles (we cycle it every ~52 generations; 3M unused rows sit right there — repetition decays in value, fresh data is free).
- Fit our own allocation curve: final quality as a function of (bootstrap steps, ES generations) from the ladder data — our-scale scaling law, heeding the fitting pitfalls (small-range extrapolation, parameter-counting choices).
- Drop-one-level probes (from the "repeatable breakthrough" pattern): pixel-level sudoku (render puzzles to bitmaps, same iterative refinement + 2D RoPE over pixel patches, answer read back as pixels — if iteration scaling survives below the symbol level, maze/ARC ports need no encoding layer); vector-valued ES fitness (per-puzzle or per-cell fitness instead of one scalar per model — the forward-only route toward the per-cell credit assignment that from-scratch ES lacks); differentiable-logic-gate variant of the iteration loop (the learned-bits rung; note our sigma already sits below bf16 resolution, so weight-bit granularity is already in play).
- Reward-modulated Hebbian probe (from the old Hebbian notes x this week's CE-floor result): per-weight eligibility traces (input-output correlations accumulated over the iteration loop, e-prop style) gated by the global fitness scalar — local credit assignment, forward-only, one rollout per update. Probe 1: fine-tune a healthy seed, compare against ES per evaluation. Probe 2 (decisive): from scratch on the tiny net where scalar-fitness ES sat at chance — if local credit is the missing ingredient, the flat line moves. Training-time only; deployed weights stay frozen.

## Publication track (parked 2026-07-05, revisit deliberately)
- Publish the best and most stable sotaku: the settledness model (96.8 @1024 / 96.8 @2048 / 96.6 @4096, model_es_settle.pt) is the candidate artifact; the four batch-two results to fold into the story: settledness fitness (flat past graded horizon — unique), fitness-at-256 (4x horizon transfer, cost-reach dial), CE phase two (uniform floor is a trap for scalar fitness), stream growth (function-preserving mechanics work, no acceleration — signal-limited not capacity-limited). GitHub release + README overhaul when we return to this.
- Tiny-model hyperparameters are inherited from d=128 without width correction (LR especially — optimal LR shifts with width, the muP thread); before concluding from any flat tiny-model probe, sweep LR. Also: a budget-matched ladder (each rung trained on its own completed schedule, like exp_tiny_bootstrap does at 5K) would upgrade the ES-takeover ladder from lower bounds to real measurements.
- Pixel program next rungs (the drop-one-level flagship twitched: iteration scaling survives below the symbol level, 371/1000 peak at 128-iter from raw glyph bitmaps, and the anneal-tail turbulence reproduced in pixel space): longer pixel training toward digit-model parity; the ES/settledness stack applied to the pixel model; the maze renderer port (generality test, no encoding layer). Also: hebbft's consistent downhill drift is unexplained signal — a v2 with a better local post-factor is a cheap iteration.
- Infra migration (after the friend answers on the 2h cap; the issues doc now lives in tips-for-running-viridian/issues_and_features.md): move training AND evals to Viridian as the default — needs a Viridian eval wrapper (~half day; checkpoints then live on R2 and --seed-model carries models into jobs) and, if the cap stays, a cloud-side auto-chainer (a scheduled function holding the R2 + vd keys, polling exported telemetry, resubmitting with --resume-checkpoint-key; strictly more reliable than session wakeups, which die with the session and stall on laptop sleep). In-job self-chaining is ruled out: jobs deliberately hold no credentials. Meanwhile: throttle Modal eval batches — modal_eval is spawn-based, so loops fire everything concurrently.
